<% if @user.errors.size > 0 %>
	window.location = "<%= edit_account_path(@user.account_id)%>"
<% else %>
	$("#<%= @user.id %>").effect("highlight", {color: "red"}, 1500);
	$("#<%= @user.id %>").remove();
<% end %>