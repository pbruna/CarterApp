<div id="main-bar">
<ul class="nav nav-pills">
  <li><%= menu_link("dashboards", "Dashboard") %></li>
  <li><%= menu_link("requests", "Correos")%></li>
  <li><%= menu_link("account", "Mi Cuenta", current_account.id) %></li>
  <% if current_user.root? %>
	<%= manage_accounts_link() %>
  <% end %>
<% flash.each do |name, msg| %>
  <div class="span6 flash-message  alert alert-<%= name == :notice ? "success" : "error" %>">
    <a class="close" data-dismiss="alert">×</a>
    <%= msg %>
  </div>
<% end %>
</ul>
</div>