<% if @account.first_login? %>
	<%= render "welcome_box" %>
<% end %>

<div id="account-overview" class="row-fluid">
	<h1>
		<%= @account.name %>
		<% if @account.trial? %>
			<small> (quedan <%= @account.trial_days_left %> días)</small>
		<% end %>
	</h1>
	<%= render "account_info" if current_user.owner? %>
	<%= render "current_user" %>
</div>

<% if @account.trial? && current_user.owner? %>
	<%= render :partial => 'select_plan' %>
<% end %>

<%= render "invoices" if current_user.owner? %>