<div style="float: right; margin-top: 5px">
	<%= link_to "<i class='icon-plus icon-white'></i> Agregar Cuenta".html_safe , new_account_path(), 
		:class => "btn btn-primary btn-small", :id => "new_account_button", "data-remote" => new_account_path(), :remote => true%>
</div>
<h2>Cuentas</h2>

<table id="accounts" class="table table-striped table-condensed">
	<tbody>
		<tr>
			<th>Nombre</th>
			<th>Id</th>
			<th>Estadísticas</th>
			<th>Acciones</th>
		</tr>
		<% @accounts.each do |account| %>
			<%= render :partial => "account", :locals => {:account => account}%>
		<% end %>
	</tbody>
</table>

<div id="account_modal"/>
<div id="user_modal"/>